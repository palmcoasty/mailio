{
  "name": "@mailio/provider-adapters",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test ./src/**/*.test.ts"
  },
  "dependencies": {
    "@mailio/shared": "*"
  }
}
